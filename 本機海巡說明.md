# 海巡搬回本機 —— 設定與運作說明

## 為什麼要這樣改

Threads 的 session cookie 誕生在你家裡的 Chrome：台灣、住宅 IP、真實瀏覽器。
Railway 在美國的資料中心 IP 上每小時重放同一份 cookie，對 Meta 來說就是
「同一個帳號的 session 每小時從地球另一端出現一次，而且從不接受伺服器換發的新值」——
這是 session 遭竊的典型特徵，所以它把 session 全域作廢（不是擋 IP，是連你本機
用同一份 cookie 也登不進去）。

實測對照：排程關閉時同一份 cookie 撐約 **4 週**；開成 10 分鐘一次只撐 **13 小時**。
調頻率只是延後死亡時間，這次改的是兩個根因。

## 這次改了什麼

### 1. cookie 會回寫了（`cookie_store.py`）

以前每次啟動都從環境變數 `THREADS_COOKIES` 載入同一份靜態 cookie，瀏覽器在這次
session 收到的新值（Meta 會不斷 rotate `sessionid` 等）在 `browser.close()` 時全部
蒸發，下次又把舊值送回去。

現在：環境變數只是**種子**，實際使用的是存在 Railway Volume（`state.db` 的 kv 表）
裡、每次跑完都回寫的最新版本。你在 Railway 貼上新的 `THREADS_COOKIES` 時會自動
偵測到指紋改變並改用新種子，所以手動換 cookie 仍然有效。

這條不必搬本機也生效，local 模式一樣受惠。

### 2. 瀏覽器動作可以搬回筆電（`local_worker.py`）

| | Railway | 本機筆電 |
|---|---|---|
| 排程、去重、AI 判斷、Telegram 推播 | ✅ | |
| 發文、留言（官方 API，穩定，不搬） | ✅ | |
| 海巡搜尋、UI 回覆（需要登入瀏覽器） | | ✅ |

方向是**本機主動連出去領工**，不是 Railway 連進你家 —— 只需要一條 outbound HTTPS，
家用網路的防火牆與浮動 IP 都不用處理。

筆電關機時：任務留在佇列，6 小時後自動清掉。海巡會少跑幾次，
但 cookie 不會被作廢。這個取捨是刻意的（`BROWSER_WORKER_FALLBACK_LOCAL=1`
可以改成退回 Railway 自己爬，但那正是殺 cookie 的那條路）。

## 設定步驟

### Railway 端

環境變數新增：

```
BROWSER_WORKER_MODE=remote
BROWSER_WORKER_TOKEN=<自己隨機產生一串長字元>
```

不設 `BROWSER_WORKER_MODE`（或設成 `local`）＝維持原本行為，隨時可以切回去。

### 本機端

1. 複製 `.env.worker.example` 成 `.env.worker`，填三個值：
   - `BOT_URL`：Railway 服務網址
   - `BROWSER_WORKER_TOKEN`：與 Railway 那邊同一串
   - `THREADS_COOKIES`：從已登入的 Chrome 用 Cookie-Editor 匯出 threads.com 整包 JSON
2. 雙擊 `start_worker.bat`
3. 視窗保持開著（關掉＝海巡停擺）

### 確認有沒有在跑

- Telegram 打「狀態」→ 會顯示 cookie 健康度與本機 worker 是否在線
- 瀏覽器開 `<BOT_URL>/worker/status`（手機也能看）

## 開機自動啟動（選用）

工作排程器 → 建立基本工作 → 觸發程序「登入時」→ 動作「啟動程式」→
選 `start_worker.bat`，「起始於」填專案資料夾。

## 還沒做、但值得做的

- **真實 Chrome profile**：`launch_persistent_context` 指向一份專用 profile，
  cookie 完全交給瀏覽器維護，連種子都不必再貼。
- **降低機器感**：排程時間加抖動、`_mark_replies` 的 6 條並行 fetch 降到 2–3 條
  （真人不會一秒開六個貼文頁）。
- **官方 `keyword_search` API**：需要 Meta 企業審核，通過後海巡完全不需要 cookie，
  本機 worker 也可以退場。
